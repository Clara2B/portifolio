.footer {
    padding: 20px 16px;
    width: 100%;
    background: var(--primary);
    box-shadow: 0 -2px 6px var(--shadow-img);
  }
  
.rodape p {
    flex-direction: column;
    align-items: center;
    text-align: center;
    color: white;
}